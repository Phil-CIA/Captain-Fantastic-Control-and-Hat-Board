# Footprints

Put project-specific KiCad footprint libraries here as `.pretty` folders.

Example for this work:
- `Snapmagic_TI_TPS1H200A_Q1.pretty/`

This file exists so the folder is tracked and visible in git/GitHub.